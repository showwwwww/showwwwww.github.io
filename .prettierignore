# Build outputs and caches
_site/
.jekyll-cache/
.sass-cache/
.jekyll-metadata

# Dependencies / vendor
vendor/
node_modules/

# Lockfiles
Gemfile.lock
package-lock.json

# Liquid-heavy templates — Prettier can mangle Liquid syntax
_layouts/
_includes/
404.html

# Site config — preserve comments and YAML structure
_config.yml

# Cached external docs for agent context — keep upstream layout intact,
# do not let Prettier reflow them.
docs/references/

# Codex CLI config (TOML, hand-authored with comments)
.codex/config.toml
